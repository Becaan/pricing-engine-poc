template header
id
ifcondition
thencondition

import com.pricing.engine.poc.model.Product;
import com.pricing.engine.poc.model.Rule;

template "tmp1"

rule "@{id}"
  dialect "mvel"
  when
    @{ifcondition};
  then
    @{thencondition};
end

end template